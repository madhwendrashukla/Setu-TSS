const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const sources = [ { slug: 'contact_section', label: 'General Inquiry' }, { slug: 'transformation_interest', label: 'Incubator Program' }, { slug: 'fundraising_workshop', label: 'Fundraising Workshop' }, { slug: 'ai_workshop', label: 'AI Workshop' }, { slug: 'apply_mentor', label: 'Apply as Mentor' } ]; for (const s of sources) { await prisma.leadSource.upsert({ where: { slug: s.slug }, update: {}, create: s }); } console.log('Seeded sources!'); } main().catch(console.error).finally(() => prisma.$disconnect());
